# Payment Service Configuration
# This service uses Cash on Delivery (COD) - no payment gateway required

# Port Configuration (optional, defaults to 8002)
PORT=8002

# CORS Origins
CORS_ORIGINS=http://localhost:3002,http://localhost:3003

# Kafka Configuration
KAFKA_BROKERS=localhost:9094,localhost:9095,localhost:9096